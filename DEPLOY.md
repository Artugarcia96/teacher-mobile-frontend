# Despliegue en el VPS

Cada push a `front-initial` despliega el frontend con GitHub Actions
(`.github/workflows/deploy.yml` → `deploy/deploy.sh`).

La guía completa (secretos, preparación del VPS y nginx) está en
`DEPLOY.md` del repo teacher-mobile-backend.
