// TODO(slice B): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function TodayPage() {
  return <Page title="Hoy"><p className="muted">En construcción.</p></Page>;
}
