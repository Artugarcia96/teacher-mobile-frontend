// TODO(slice D): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function ReviewPage() {
  return <Page title="Revisar"><p className="muted">En construcción.</p></Page>;
}
