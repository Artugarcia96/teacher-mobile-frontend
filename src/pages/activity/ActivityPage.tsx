// TODO(slice D): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function ActivityPage() {
  return <Page title="Actividad"><p className="muted">En construcción.</p></Page>;
}
