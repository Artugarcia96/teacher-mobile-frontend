// TODO(slice A): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function LoginPage() {
  return <Page title="Entrar"><p className="muted">En construcción.</p></Page>;
}
