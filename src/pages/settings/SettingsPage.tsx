// TODO(slice A): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function SettingsPage() {
  return <Page title="Ajustes"><p className="muted">En construcción.</p></Page>;
}
