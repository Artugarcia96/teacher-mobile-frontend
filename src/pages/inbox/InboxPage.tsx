// TODO(slice C): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function InboxPage() {
  return <Page title="Evaluar"><p className="muted">En construcción.</p></Page>;
}
