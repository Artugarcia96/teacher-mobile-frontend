// TODO(slice A): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function CoursesPage() {
  return <Page title="Clases"><p className="muted">En construcción.</p></Page>;
}
