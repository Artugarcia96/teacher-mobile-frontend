// TODO(slice E): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function UnitPage() {
  return <Page title="Unidad"><p className="muted">En construcción.</p></Page>;
}
