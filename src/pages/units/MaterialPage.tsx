// TODO(slice E): implement per docs/PRODUCT.md. Stub so the app compiles.
import { Page } from '../../ui';

export default function MaterialPage() {
  return <Page title="Material"><p className="muted">En construcción.</p></Page>;
}
