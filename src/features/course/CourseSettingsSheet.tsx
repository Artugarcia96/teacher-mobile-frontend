// TODO(slice A): class settings sheet — subject, room, color, horario (slots), ponderaciones (categories), archivar.
import type { CourseDetail } from '../../api/types';

export default function CourseSettingsSheet(_: { open: boolean; onClose: () => void; course: CourseDetail }) {
  return null;
}
