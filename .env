# Leave empty in development: Vite proxies /api to the backend (VITE_API_PROXY, default http://127.0.0.1:8000).
# In production builds set it to the backend origin, e.g. https://api.sepia.es
VITE_API_URL=
